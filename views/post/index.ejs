<html>
    <head>
        <title> <%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
<body>
    <h1><%= title %></h1>
    <div id="container">
        <% if (numero > 0) { %>
            <% for (var i = 0; i < arr.length; i++){ %>
    	<div class="post">
          <div class="intro">
          <h2> <%= arr[i].titolo %> </h2>
    			<%= arr[i].testo.substring(0,100) %>
    			<p>continua a leggere <a href="./articolo/<%= arr[i].titolo%>">qui</a></p>
    		</div>
    		<div class="who">
    			Scritto da: <%= arr[i].autor %> con i tag <%= arr[i].tag %>
    		</div>
    	</div>
            <% } %>
        <% } else { %>
            <h3><%= arr %></h3>
        <% } %>
    </div>
  </body>
</html>
