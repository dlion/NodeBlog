<!DOCTYPE html>
<html>
  <head>
      <title><%= namesite %> - <%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <div id="container">
        <% if (numero > 0) { %>
            <% for (var i = 0; i < arr.length; i++){ %>
    	<div class="post">
          <div class="intro">
          <h2> <%= arr[i].titolo %> </h2>
    			<%= arr[i].testo.substring(0,100) %>
    			<p>continua a leggere <a href="./<%= arr[i].id%>">qui</a></p>
    		</div>
    		<div class="who">
    			Scritto da: <%= arr[i].autore_id %> nella categoria <%= arr[i].categoria_id %>
    		</div>
    	</div>
            <% } %>
        <% } else { %>
            <h3><%= arr %></h3>
        <% } %>
    </div>
  </body>
</html>
